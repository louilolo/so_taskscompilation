Eu fiz uma chamada que realiza a seguinte operações:

- Usa chamadas de sistema para abrir, ler e fechar o arquivo.
- Conta as palavras enquanto lê o arquivo em blocos.
- Imprime o número total de palavras usando printf.
depois fecha

*comandos usados*
open, read, close, printf, exit
os registros rdi, rsi, rdx, rax, entre outros, eu usei pra passar argumentos para as chamadas de sistema.
